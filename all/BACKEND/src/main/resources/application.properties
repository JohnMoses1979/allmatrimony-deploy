

 spring.application.name=all-matrimony-backend
 server.port=8080

 spring.datasource.url=jdbc:mysql://13.206.220.74:3306/all_matrimony
 spring.datasource.username=root
 spring.datasource.password=giri
 spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

 spring.jpa.hibernate.ddl-auto=update
 spring.jpa.show-sql=true
 spring.jpa.properties.hibernate.format_sql=true
 spring.jpa.open-in-view=false
 spring.web.resources.static-locations=classpath:/static/,file:../../uploads/
 app.upload-dir=../../uploads

logging.level.org.springframework.security=INFO


spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=pranay12ibliss@gmail.com
spring.mail.password=qnef gujs zjap rliw
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true




# spring.application.name=${SPRING_APPLICATION_NAME}
# server.port=${SERVER_PORT}

# spring.datasource.url=${DB_URL}
# spring.datasource.username=${DB_USERNAME}
# spring.datasource.password=${DB_PASSWORD}
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.format_sql=true
# spring.jpa.open-in-view=false

# spring.web.resources.static-locations=classpath:/static/,file:./uploads/
# app.upload-dir=${UPLOAD_DIR}

# logging.level.org.springframework.security=INFO

# twilio.account-sid=${TWILIO_ACCOUNT_SID}
# twilio.auth-token=${TWILIO_AUTH_TOKEN}
# twilio.verify-sid=${TWILIO_VERIFY_SID}

# spring.mail.host=smtp.gmail.com
# spring.mail.port=587
# spring.mail.username=${MAIL_USERNAME}
# spring.mail.password=${MAIL_PASSWORD}
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.starttls.enable=true



twilio.account-sid=${TWILIO_ACCOUNT_SID:}
twilio.auth-token=${TWILIO_AUTH_TOKEN:}
twilio.phone-number=${TWILIO_PHONE_NUMBER:}

twilio.account-sid=${TWILIO_ACCOUNT_SID:}
twilio.auth-token=${TWILIO_AUTH_TOKEN:}
twilio.phone-number=${TWILIO_PHONE_NUMBER:}
